import React from "react";
import ReactDOM from "react-dom/client";
import "./Spindle/app.css";
import App from "./Spindle/App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
